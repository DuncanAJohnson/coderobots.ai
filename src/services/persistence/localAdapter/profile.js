/**
 * User profile adapter (browser-local).
 * No student-group tracking without telemetry; App.jsx skips the profile
 * check entirely when telemetry is off.
 */

export const getUserProfile = async () => null;

export const saveUserProfile = async () => null;
